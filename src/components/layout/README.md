# CRM Shell Reference Refresh

Replace the matching files in your layout components directory with the files in this package.

Updated visual behavior:
- Theme-aware translucent AppBar with a restrained blur and a single subtle primary accent line.
- Solid MUI Drawer/Sidebar with an ambient brand header, gradient brand mark, and selected-item indicator.
- Compact MUI controls for theme, language, and account actions.
- Menus use a restrained paper blur only; no custom control primitives were introduced.
- Light, dark, RTL, and LTR remain theme-driven.

No authentication, route, permission, localization, stock, data, or provider behavior was changed.
